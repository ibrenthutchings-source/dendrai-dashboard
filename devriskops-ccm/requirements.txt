# DevRiskOps CCM — Python dependencies
#
# Install with:  pip install -r requirements.txt
#
# apache-airflow has its own strict, version-pinned dependency constraints —
# installing it via a bare `pip install -r requirements.txt` can produce a
# broken environment. For dags/ccm_dag.py specifically, prefer:
#   pip install "apache-airflow==2.10.2" --constraint \
#     "https://raw.githubusercontent.com/apache/airflow/constraints-2.10.2/constraints-3.11.txt"
# then `pip install -r requirements.txt` for everything else (pip will see
# apache-airflow already satisfied and skip it).

# ── Core web/service framework (backend_api.py) ─────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# ── Database drivers ─────────────────────────────────────────────────────────
asyncpg>=0.30.0          # backend_api.py — async Postgres driver
psycopg2-binary>=2.9.9   # app.py, mcp_server.py, dags/ccm_dag.py — sync driver
sqlalchemy>=2.0.35       # app.py — pandas.read_sql_query engine

# ── Dashboard / analytics (app.py) ──────────────────────────────────────────
streamlit>=1.39.0
plotly>=5.24.0
pandas>=2.2.3
numpy>=2.1.1

# ── HTTP / integration ───────────────────────────────────────────────────────
httpx>=0.27.2
python-dotenv>=1.0.1

# ── AI agent tooling (mcp_server.py) ─────────────────────────────────────────
mcp>=1.2.0

# ── Orchestration (dags/ccm_dag.py) — see constraints-file note above ──────
apache-airflow>=2.10.2
