apiVersion: v1
kind: Pod
spec:
  containers:
    - name: app
      image: nginx:1.25.3
      securityContext:
        runAsUser: 1000
      resources:
        limits:
          cpu: "500m"
          memory: "128Mi"
