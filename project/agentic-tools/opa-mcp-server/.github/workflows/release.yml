name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write
  id-token: write
  packages: write
  attestations: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm install --no-audit --no-fund --loglevel=error
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build

  # The bundled-OPA platform packages must publish BEFORE the main package so
  # the main package's optionalDependencies resolve for the first installer.
  # These 5 are brand-new packages, so they use a granular @orygn-scoped token
  # (NPM_TOKEN) rather than OIDC; the main package below keeps OIDC untouched.
  publish-binaries:
    name: Publish platform binary packages to npm
    runs-on: ubuntu-latest
    needs: validate
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          registry-url: 'https://registry.npmjs.org'
      - run: npm install -g npm@latest
      # Defensive: ensure the 5 packages carry the release version even if the
      # release-prep commit forgot to run `npm run bundle:sync`.
      - name: Sync platform package versions to the main version
        run: node scripts/sync-versions.mjs
      - name: Fetch and checksum-verify all 5 OPA binaries
        run: node scripts/fetch-opa-binaries.mjs
      - name: Publish each platform package (skip versions already on npm)
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          set -euo pipefail
          for dir in packages/opa-mcp-*/; do
            name=$(node -p "require('./${dir}package.json').name")
            ver=$(node -p "require('./${dir}package.json').version")
            if npm view "${name}@${ver}" version >/dev/null 2>&1; then
              echo "Skip ${name}@${ver} (already published)"
            else
              echo "Publishing ${name}@${ver}"
              npm publish "${dir}" --access public
            fi
          done

  publish-npm:
    name: Publish to npm
    runs-on: ubuntu-latest
    needs: [validate, publish-binaries]
    environment:
      name: npm
      url: https://www.npmjs.com/package/@orygn/opa-mcp
    permissions:
      # `id-token: write` lets npm exchange the GitHub Actions OIDC
      # token for a short-lived publish token, per the Trusted
      # Publisher configured at
      # https://www.npmjs.com/package/@orygn/opa-mcp/access.
      # No NPM_TOKEN secret is read; setting NODE_AUTH_TOKEN at all
      # would disable OIDC, so it is deliberately absent.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
          registry-url: 'https://registry.npmjs.org'
      # OIDC trusted-publisher auth requires npm CLI >= 11.5.1; the
      # version bundled with Node 20 (npm 10.x) only does provenance,
      # not the trusted-publisher token exchange, and the publish PUT
      # falls back to the placeholder _authToken setup-node writes,
      # which the registry answers with a 404.
      - run: npm install -g npm@latest
      - run: npm install --no-audit --no-fund --loglevel=error
      - run: npm run build
      - name: Publish via OIDC (provenance auto-attached)
        run: npm publish --access public

  publish-docker:
    name: Build & push multi-arch Docker image
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Extract version
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: orygn/opa-mcp
          tags: |
            type=raw,value=latest
            type=raw,value=${{ steps.version.outputs.version }}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Update Docker Hub description
        uses: peter-evans/dockerhub-description@v5
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: orygn/opa-mcp
          readme-filepath: ./DOCKER_HUB_DESCRIPTION.md

  build-mcpb:
    name: Build MCPB bundle
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm install --no-audit --no-fund --loglevel=error
      - run: npm run build
      - name: Prune to production dependencies
        run: npm prune --omit=dev
      - name: Stage bundle
        run: |
          set -eux
          mkdir -p bundle/server
          cp -r dist bundle/server/dist
          cp package.json bundle/server/package.json
          cp -r node_modules bundle/server/node_modules
          # The OPA binary ships as platform-specific optionalDependencies for the
          # npm/npx install path. The .mcpb is a single frozen artifact built on one
          # OS, so a bundled binary would be the wrong platform for most people who
          # download it and pushes the zip past Smithery's 25 MB limit. Drop the
          # platform packages here; the bundle resolves `opa` from OPA_BINARY or PATH,
          # exactly as it did before 0.2.0.
          rm -rf bundle/server/node_modules/@orygn/opa-mcp-*
          cp manifest.json bundle/manifest.json
          cp README.md bundle/README.md
          cp LICENSE bundle/LICENSE
      - name: Pack .mcpb (zip)
        run: |
          cd bundle
          zip -r ../opa-mcp.mcpb . -x "*.DS_Store" "Thumbs.db"
      - name: Compute sha256
        run: |
          sha256sum opa-mcp.mcpb > opa-mcp.mcpb.sha256
          cat opa-mcp.mcpb.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: mcpb-bundle
          path: |
            opa-mcp.mcpb
            opa-mcp.mcpb.sha256
          retention-days: 30

  publish-smithery:
    name: Publish to Smithery
    runs-on: ubuntu-latest
    needs: build-mcpb
    environment:
      name: smithery
      url: https://smithery.ai/server/@orygn/opa-mcp
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: mcpb-bundle
      - name: Install Smithery CLI
        run: npm install -g @smithery/cli
      - name: Publish bundle
        run: smithery mcp publish ./opa-mcp.mcpb -n @orygn/opa-mcp
        env:
          SMITHERY_API_KEY: ${{ secrets.SMITHERY_API_KEY }}

  publish-registry:
    name: Publish to MCP Registry
    runs-on: ubuntu-latest
    # The registry verifies every package in server.json is already published and
    # that we own the io.github.OrygnsCode namespace, so npm + Docker must land
    # first. Nothing depends on this job, so a registry hiccup (it is still in
    # preview) never blocks the rest of the release.
    needs: [publish-npm, publish-docker]
    permissions:
      # GitHub OIDC proves the identity behind the io.github.OrygnsCode/* namespace,
      # so no token is stored. contents:read is enough to read server.json.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v7
      # server.json has no auto-sync (unlike the platform packages), so guard
      # against a release-prep commit that forgot to bump it. A mismatch would
      # otherwise publish a stale version or get rejected with a cryptic error.
      - name: Verify server.json versions match the tag
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME#v}"
          top=$(jq -r '.version' server.json)
          npmv=$(jq -r '.packages[] | select(.registryType=="npm") | .version' server.json)
          oci=$(jq -r '.packages[] | select(.registryType=="oci") | .identifier' server.json)
          echo "tag=${tag}  server.json: version=${top} npm=${npmv} oci=${oci}"
          [ "${top}" = "${tag}" ]  || { echo "::error::server.json .version (${top}) != tag (${tag})"; exit 1; }
          [ "${npmv}" = "${tag}" ] || { echo "::error::server.json npm package version (${npmv}) != tag (${tag})"; exit 1; }
          case "${oci}" in *":${tag}") ;; *) echo "::error::server.json oci identifier (${oci}) is not pinned to :${tag}"; exit 1 ;; esac
      - name: Install mcp-publisher
        run: |
          set -euo pipefail
          curl -fsSL "https://github.com/modelcontextprotocol/registry/releases/latest/download/mcp-publisher_$(uname -s | tr '[:upper:]' '[:lower:]')_$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/').tar.gz" | tar xz mcp-publisher
      - name: Authenticate via GitHub OIDC
        run: ./mcp-publisher login github-oidc
      - name: Publish server.json to the MCP Registry
        run: ./mcp-publisher publish

  github-release:
    name: GitHub Release
    runs-on: ubuntu-latest
    # Smithery is a secondary channel published from the same artifact; a hiccup
    # there must not block the canonical GitHub release, which does not depend on
    # the Smithery upload having succeeded.
    needs: [publish-npm, publish-docker, build-mcpb]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v4
        with:
          name: mcpb-bundle
      - name: Extract release notes from CHANGELOG
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          {
            echo "## What's new in ${GITHUB_REF_NAME}"
            echo ""
            awk "/^\#\# \[${VERSION}\]/{found=1; next} found && /^\#\# \[/{exit} found{print}" CHANGELOG.md
          } > release-notes.md
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          body_path: release-notes.md
          files: |
            opa-mcp.mcpb
            opa-mcp.mcpb.sha256
